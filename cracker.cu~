#include <stdio.h>
#include <openssl/md5.h>

#define THREADS_PER_BLOCK 64

__global__ void md5-calc(char* passGuess, char* guessHash) {
  MD5((unsigned char*) passGuess, strlen(passGuess), guessHash);
}

int main() {
  kernel<<<1,1>>>();
  cudaDeviceSynchronize();
  return 0;
}
